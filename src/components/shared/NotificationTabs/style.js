import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles( theme => ({
    root:{
       
    },
    tabsWrapper:{
        // boxShadow:'0px 4px 20px rgba(81, 81, 81, 0.1)'
    }

}))

export default useStyles